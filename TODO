-Reorganiser le makefile et les headers
-Faire des tests avec funcheck/ulimit pour les fonctions utilisant la liballoc
-ajouter un branche dev
-ajouter un testeur qui permettra de push uniquement si la libft est a la norme + si le testeur est positif
-Finir la sous-bibliotheque de vector (Verif)
-Merge master avec vector
-Ajouter les fichiers ft_strappend.c et ft_strnappend.c dans le Makefile
-Ajouter la fonction realloc a libft.h
-Implementer la fonction xrealloc
-Erreur dans realdine a la ligne qui contient : original_size <= size; supprimer la condition
